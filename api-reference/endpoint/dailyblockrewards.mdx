---
title: "Get Daily Total Block Rewards"
sidebarTitle: "Daily Block Rewards"
description: "Retrieve the total ETH block rewards distributed to miners per day over a specified date range. Requires a Standard plan API key."
---

Use this endpoint to track the total block rewards issued each day across a date range. Unlike the [daily block count](/api-reference/endpoint/dailyblkcount) endpoint, this one focuses purely on the reward totals in ETH, making it useful for emissions analysis, miner revenue modeling, and historical monetary policy research.

<Note>
  This is a PRO endpoint, available on the [Standard Plan](/rate-limits) and above.
</Note>

## Query Parameters

<ParamField query="chainid" type="string" required>
  The chain ID to query. For example, `1` for Ethereum mainnet or `42161` for Arbitrum. See the full list of [supported chains](/supported-chains).
</ParamField>

<ParamField query="module" type="string" required default="stats">
  Set to `stats` for this endpoint.
</ParamField>

<ParamField query="action" type="string" required default="dailyblockrewards">
  Set to `dailyblockrewards` for this endpoint.
</ParamField>

<ParamField query="startdate" type="string" required>
  Start of the date range in `yyyy-MM-dd` format. Example: `2019-02-01`.
</ParamField>

<ParamField query="enddate" type="string" required>
  End of the date range in `yyyy-MM-dd` format. Example: `2019-02-28`.
</ParamField>

<ParamField query="sort" type="string">
  Sort order for results. Use `asc` for ascending or `desc` for descending. Defaults to `asc`.
</ParamField>

<ParamField query="apikey" type="string" required>
  Your Etherscan API key. [Set one up](/set-up-your-api-key) if you don't have one.
</ParamField>

## Response Fields

<ResponseField name="status" type="string">
  `1` if the request returned data, `0` otherwise.
</ResponseField>

<ResponseField name="message" type="string">
  `OK` on success, or an error description such as `NOTOK`.
</ResponseField>

<ResponseField name="result" type="array">
  Array of daily block reward data points.
  <Expandable title="result item fields">
    <ResponseField name="UTCDate" type="string">
      The date of the data point in `YYYY-MM-DD` format (UTC).
    </ResponseField>
    <ResponseField name="unixTimeStamp" type="string">
      Unix timestamp (seconds) for the start of that UTC day.
    </ResponseField>
    <ResponseField name="blockRewards_Eth" type="string">
      Total block rewards distributed to miners on that day, in ETH.
    </ResponseField>
  </Expandable>
</ResponseField>

## Example

<CodeGroup>
```bash cURL
curl "https://api.etherscan.io/v2/api\
?chainid=1\
&module=stats\
&action=dailyblockrewards\
&startdate=2019-02-01\
&enddate=2019-02-28\
&sort=asc\
&apikey=YOUR_API_KEY"
```

```javascript JavaScript
const params = new URLSearchParams({
  chainid: "1",
  module: "stats",
  action: "dailyblockrewards",
  startdate: "2019-02-01",
  enddate: "2019-02-28",
  sort: "asc",
  apikey: "YOUR_API_KEY",
});

const response = await fetch(`https://api.etherscan.io/v2/api?${params}`);
const data = await response.json();
console.log(data.result);
```

```python Python
import requests

params = {
    "chainid": "1",
    "module": "stats",
    "action": "dailyblockrewards",
    "startdate": "2019-02-01",
    "enddate": "2019-02-28",
    "sort": "asc",
    "apikey": "YOUR_API_KEY",
}

response = requests.get("https://api.etherscan.io/v2/api", params=params)
print(response.json()["result"])
```
</CodeGroup>

```json Example Response
{
  "status": "1",
  "message": "OK",
  "result": [
    {
      "UTCDate": "2019-02-01",
      "unixTimeStamp": "1548979200",
      "blockRewards_Eth": "15300.65625"
    },
    {
      "UTCDate": "2019-02-02",
      "unixTimeStamp": "1549065600",
      "blockRewards_Eth": "15611.625"
    }
  ]
}
```
