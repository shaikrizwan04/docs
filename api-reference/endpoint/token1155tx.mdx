---
title: "Get ERC-1155 Multi-Token Transfer Events by Address"
sidebarTitle: "token1155tx"
description: "Retrieve ERC-1155 multi-token transfer events for an address or contract, with tokenID, tokenValue, and standard pagination support."
---

Use this endpoint to fetch ERC-1155 multi-token transfer events. ERC-1155 contracts support both fungible and non-fungible tokens in a single contract, so each transfer record includes both a `tokenID` and a `tokenValue` indicating how many units of that token ID were moved. At least one of `address` or `contractaddress` is required.

<Note>
  ERC-1155 transfers can represent fungible tokens, NFTs, or a batch of both. Always check both `tokenID` and `tokenValue` to understand the full transfer.
</Note>

## Parameters

<ParamField query="chainid" type="string" required>
  The chain ID to query. Use `1` for Ethereum mainnet or any chain from the [supported chains](/supported-chains) list.
</ParamField>

<ParamField query="module" type="string" required default="account">
  Set to `account` for this endpoint.
</ParamField>

<ParamField query="action" type="string" required default="token1155tx">
  Set to `token1155tx` for this endpoint.
</ParamField>

<ParamField query="contractaddress" type="string">
  The ERC-1155 contract address to filter transfers by. Provide at least one of `address` or `contractaddress`.
</ParamField>

<ParamField query="address" type="string">
  The account address to filter transfers by. Provide at least one of `address` or `contractaddress`.
</ParamField>

<ParamField query="startblock" type="integer" default="0">
  Starting block number to search from.
</ParamField>

<ParamField query="endblock" type="integer" default="999999999">
  Ending block number to search to.
</ParamField>

<ParamField query="page" type="integer" default="1">
  Page number for pagination.
</ParamField>

<ParamField query="offset" type="integer" default="10">
  Number of records per page.
</ParamField>

<ParamField query="sort" type="string" default="asc">
  Sort order. Use `asc` for oldest first, `desc` for latest first.
</ParamField>

<ParamField query="apikey" type="string" required>
  Your Etherscan API key. [Set one up](/set-up-your-api-key) if you don't have one.
</ParamField>

## Response

<ResponseField name="status" type="string">
  `1` if the request returned data, `0` otherwise.
</ResponseField>

<ResponseField name="message" type="string">
  `OK` on success. See [common error messages](/common-error-messages) for error values.
</ResponseField>

<ResponseField name="result" type="array">
  An array of ERC-1155 transfer objects.

  <Expandable title="result[]">
    <ResponseField name="blockNumber" type="string">Block number in which the transaction was included.</ResponseField>
    <ResponseField name="timeStamp" type="string">Time the block was mined, as a UNIX timestamp in seconds.</ResponseField>
    <ResponseField name="hash" type="string">The transaction hash.</ResponseField>
    <ResponseField name="nonce" type="string">Number of transactions sent from the sender before this one.</ResponseField>
    <ResponseField name="blockHash" type="string">Hash of the block containing the transaction.</ResponseField>
    <ResponseField name="transactionIndex" type="string">Index position of the transaction within the block.</ResponseField>
    <ResponseField name="gas" type="string">Maximum gas the sender allowed.</ResponseField>
    <ResponseField name="gasPrice" type="string">Price paid per unit of gas, in wei.</ResponseField>
    <ResponseField name="gasUsed" type="string">Units of gas actually consumed.</ResponseField>
    <ResponseField name="cumulativeGasUsed" type="string">Total gas used up to and including this transaction in the block.</ResponseField>
    <ResponseField name="input" type="string">Input data sent with the transaction.</ResponseField>
    <ResponseField name="methodId" type="string">First four bytes of input data identifying the called function.</ResponseField>
    <ResponseField name="functionName" type="string">Decoded function signature when the contract is verified.</ResponseField>
    <ResponseField name="contractAddress" type="string">Contract address of the ERC-1155 token.</ResponseField>
    <ResponseField name="from" type="string">Address that sent the tokens.</ResponseField>
    <ResponseField name="to" type="string">Address that received the tokens.</ResponseField>
    <ResponseField name="tokenID" type="string">The token ID within the ERC-1155 contract that was transferred.</ResponseField>
    <ResponseField name="tokenValue" type="string">Number of units of the given `tokenID` transferred in this event.</ResponseField>
    <ResponseField name="tokenName" type="string">Name of the token collection.</ResponseField>
    <ResponseField name="tokenSymbol" type="string">Symbol of the token collection.</ResponseField>
    <ResponseField name="confirmations" type="string">Number of blocks mined after the block containing this transaction.</ResponseField>
  </Expandable>
</ResponseField>

## Example

<CodeGroup>
```bash cURL
curl "https://api.etherscan.io/v2/api\
?chainid=1\
&module=account\
&action=token1155tx\
&contractaddress=0x76be3b62873462d2142405439777e971754e8e77\
&address=0x83f564d180b58ad9a02a449105568189ee7de8cb\
&startblock=0\
&endblock=999999999\
&page=1\
&offset=10\
&sort=asc\
&apikey=YOUR_API_KEY"
```

```python Python
import requests

params = {
    "chainid": "1",
    "module": "account",
    "action": "token1155tx",
    "contractaddress": "0x76be3b62873462d2142405439777e971754e8e77",
    "address": "0x83f564d180b58ad9a02a449105568189ee7de8cb",
    "startblock": 0,
    "endblock": 999999999,
    "page": 1,
    "offset": 10,
    "sort": "asc",
    "apikey": "YOUR_API_KEY",
}

response = requests.get("https://api.etherscan.io/v2/api", params=params)
print(response.json())
```

```javascript JavaScript
const params = new URLSearchParams({
  chainid: "1",
  module: "account",
  action: "token1155tx",
  contractaddress: "0x76be3b62873462d2142405439777e971754e8e77",
  address: "0x83f564d180b58ad9a02a449105568189ee7de8cb",
  startblock: "0",
  endblock: "999999999",
  page: "1",
  offset: "10",
  sort: "asc",
  apikey: "YOUR_API_KEY",
});

const response = await fetch(`https://api.etherscan.io/v2/api?${params}`);
const data = await response.json();
console.log(data);
```
</CodeGroup>

```json Response
{
  "status": "1",
  "message": "OK",
  "result": [
    {
      "blockNumber": "13472395",
      "timeStamp": "1634973285",
      "hash": "0x643b15f3ffaad5d38e33e5872b4ebaa7a643eda8b50ffd5331f682934ee65d4d",
      "nonce": "41",
      "blockHash": "0xa5da536dfbe8125eb146114e2ee0d0bdef2b20483aacbf30fed6b60f092059e6",
      "transactionIndex": "100",
      "gas": "140000",
      "gasPrice": "52898577246",
      "gasUsed": "105030",
      "cumulativeGasUsed": "11739203",
      "input": "deprecated",
      "methodId": "0x3e6b214b",
      "functionName": "",
      "contractAddress": "0x76be3b62873462d2142405439777e971754e8e77",
      "from": "0x1e63326a84d2fa207bdfa856da9278a93deba418",
      "to": "0x83f564d180b58ad9a02a449105568189ee7de8cb",
      "tokenID": "10371",
      "tokenValue": "1",
      "tokenName": "parallel",
      "tokenSymbol": "LL",
      "confirmations": "9995266"
    }
  ]
}
```
